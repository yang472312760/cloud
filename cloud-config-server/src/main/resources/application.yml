server:
  port: 3344
#eureka:
#  client:
#    service-url:
#      defaultZone:  http://localhost:7001/eureka
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yang472312760/cloud-config.git
          search-paths:
            - cloud-config
        label: main